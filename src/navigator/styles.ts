import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    flex: 1,
  },
  icon: {
    paddingLeft: 10,
    fontSize: 32,
  },
  tab: {
    backgroundColor: '#32A2F2',
  },
});

export default styles;
